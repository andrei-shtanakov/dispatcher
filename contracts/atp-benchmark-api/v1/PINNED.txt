source: atp-platform packages/atp-dashboard/atp/dashboard/v2/factory.py
        (create_app().openapi(), ATP_SERVER_PROFILE=eco), pruned to the
        GET paths dispatcher consumes (scripts/prune_atp_openapi.py)
commit: da3a264e0ec73811b5f066e47a343f2e91600b91
vendored: 2026-08-16
fixtures: rows were ORM-seeded into an isolated sqlite (the auth-gated HTTP
  write flow was bypassed to seed them) — the response bytes themselves are
  real endpoint output, captured from the running eco app.
  run-status-completed.json / run-status-in-progress.json: authored,
  schema-validated (runbook §3 fallback, 2026-08-16) — shaped after
  RunStatusResponse in this directory's openapi.json and the producer's
  score_contract.run_score_semantics; a live capture would need a seeded
  completed run and a minted token, cost not paid for phase-2 PR-2.
note: pinned copy (repo-boundaries vendoring, ADR-ECO-003). Do not edit here —
  re-vendor with scripts/revendor_atp_benchmark_api.sh, which derives every
  value in this directory from the one commit it is given. Procedure:
  docs/revendor-atp-benchmark-api.md. Nothing in shipped code may read
  ../atp-platform at run time.

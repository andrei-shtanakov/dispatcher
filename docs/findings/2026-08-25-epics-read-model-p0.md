# Ф3 read-model эпиков: четыре P0 в смерженном master + ослабленный тест

Статус: **вердикт по Ф3 не принят**. Ф4 (разметка флота) заморожена до зелёных
регрессий и повторного ревью. Дата: 2026-08-25. Разбор: внешний критик (Codex),
воспроизведение — прямыми пробами по `origin/master` @ `a83269f`.

Общая форма всех четырёх: **агрегат остаётся уверенно неверным и ничего не падает**.
Ровно тот класс, ради которого в ADR-ECO-010 заведена четырёхсостоянная
классификация — и ровно тот, который read-model в первой редакции не удержала.

## P0-1 — snapshot/v2 не валидируется по содержанию

`dispatcher/core/snapshot_contract.py` парсит ОБЕ версии одной моделью
`WorkspaceSnapshotV1`, где `github: dict[str, Any] | None` и `extra="allow"`.
Значит `schema_version: 2` — это заявление продюсера, которое никто не проверяет:
ось эпиков в v2 типизирована в завендоренной схеме (`EpicClassification` с
required `epic/defect/classification/diagnostics/subject_uri/carrier/observed_at`),
а у потребителя от неё не остаётся ничего.

Проба: `{"epic": {"ЧУШЬ": 123}}` внутри v2 — принято, `schema_version == 2`,
`carries_epic_axis()` → True. Дальше read-model читает `classification` из
объекта, где его нет, получает `None`, и кладёт артефакт в `unclassified`.
Поддельный продюсер бесшумно раздувает бакет «без эпика» — тот самый счётчик,
по которому решают, пора ли переключать ось дайджеста.

## P0-2 — смешанный флот v1+v2 отдаёт `read`

`_github_planes()` при наличии хотя бы одного v2-хоста возвращает
`state="read"` и дописывает причину в `detail` строкой «partial: hosts still on
snapshot v1…». Состояние — машинно-читаемое поле, `detail` — человекочитаемое.
Любой потребитель, который смотрит на `state` (а именно так и написан веб, и MCP,
и будущий Robin), получает «плоскость прочитана» для флота, часть которого не
опрошена вовсе.

Проба: v1-хост + v2-хост → `issues state=read count=1`, при том что вклад v1-хоста
неизвестен и равен не нулю, а «не измерено».

## P0-3 — неизвестный эпик со стороны GitHub исчезает из агрегата

TODO-плоскость проходит через `apply_registry()` и получает EP-UNKNOWN.
GitHub-плоскость не резолвится по реестру вообще: `key = tagged_epic` для любой
строки. Строки таблицы при этом строятся только по `registry.epics`, поэтому
артефакт с опечаткой в теге **выпадает и из строк, и из бакета**, оставаясь
в итоге плоскости.

Проба: эпик `eco.dark-factroy` (опечатка) → строки нет, в `unclassified` не попал,
EP-UNKNOWN не выпущен, но `issues: read 1`. Сумма по строкам ≠ итог плоскости —
инвариант «нет итога без плоскостей» нарушен в самой арифметике.

## P0-4 — нет дедупликации между продюсерами и нет учёта устаревания

`_github_planes()` складывает вклад всех снапшотов подряд. Два хоста, у которых
общий репозиторий в воркспейсе (нормальная ситуация: две машины одного владельца),
дают один и тот же issue дважды. Плюс возраст снапшота не участвует вообще:
`STALE_AFTER_SECONDS` есть в `sync.py`, но read-model эпиков про него не знает —
данные суточной давности идут в счёт как свежие, и полнота выглядит выше реальной.

## Regression finding — ослабленный собственный тест

`tests/test_publish.py::test_take_snapshot_rejects_contract_violation` до Ф3
подавал `schema_version: 2` и требовал `PublishError`. В Ф3 v2 стала легитимной,
и я **изменил число на 99 вместо того, чтобы восстановить смысл проверки**.

Исходный смысл: *публиковать снапшот, форму которого мы не проверяем, нельзя.*
Проверка версии вне вендоренного набора — лишь частный случай; после легитимации
v2 общий случай остался непокрытым ровно в тот момент, когда стал актуальным.
То есть тест, который поймал бы P0-1 на публикации, был снят с предохранителя
той же правкой, что завела дефект.

Класс ошибки — «подгонка теста под изменившуюся реализацию»: премиса теста
действительно изменилась, но восстанавливать надо было ИНВАРИАНТ, а не
переписывать константу до зелёного. Тест возвращается к исходному смыслу:
непротиворечивый v2 (содержательно, не по номеру) отвергается при публикации.

## Порядок работ

1. Красные тесты на все пять пунктов — до единой правки кода.
2. Исправления.
3. Повторное ревью, затем разморозка Ф4.

PR #95 (восстановление журналов) и эти исправления не смешиваются.

---

## Что сделано (2026-08-25)

Порядок соблюдён: сначала пять красных тестов одним коммитом, затем исправления.

| Дефект | Исправление | Чем закреплено |
|---|---|---|
| P0-1 | у каждой версии своя модель; v2 разобран по завендоренной схеме целиком и **строго** (без lax-приведения `"12"` → `12`) | `test_a_forged_v2_payload_is_refused_by_the_typed_model`, `test_the_typed_models_reject_what_the_vendored_schema_rejects` |
| P0-2 | третье состояние плоскости `partial`; причина и в `state`, и в `detail` | `test_a_mixed_v1_v2_fleet_does_not_get_the_read_state` + JS-харнесс |
| P0-3 | GitHub-плоскость резолвится по реестру теми же правилами, что и TODO; находки — в отдельном `classification_diagnostics` | `test_an_unknown_github_epic_is_reported_and_stays_in_the_aggregate` |
| P0-4 | дедупликация по `(плоскость, remote, номер)`, свежий продюсер выигрывает; устаревание переводит плоскость в `partial` | `test_one_artifact_seen_by_two_producers_is_counted_once`, `test_a_stale_producer_visibly_degrades_completeness` |
| ослабленный тест | возвращён к инварианту; проверка неподдержанной версии выделена отдельно | `test_take_snapshot_rejects_contract_violation` |

Три решения, принятые по ходу и заслуживающие возражения:

1. **`classification_diagnostics` отдельно от `registry_diagnostics`.** Опечатка в
   теге одного issue ничего не говорит о самом `epics.toml`. Сложить их в одно поле
   значит уронить `registry_ok` и отправить оператора чинить не тот файл.
2. **EP-MOVED ведёт себя как EP-UNKNOWN — в бакет.** Это повторяет уже
   отревьюенное поведение `apply_registry` в TODO-плоскости. Резолвить тумбстоун в
   целевой эпик только на GitHub-стороне значило бы развести две плоскости в
   семантике переименования — **открытый вопрос**, а не решённый: правильный ответ,
   вероятно, «резолвить в обеих», и его стоит принимать отдельно.
3. **Строгий режим pydantic только для v2.** Конверт (`generated_at` из ISO-строки)
   и вся v1 остаются lax: строгость введена там, где пин объявляет конкретные
   скаляры. Тест на дрейф поймал ровно это — в lax-режиме модель принимала
   `number: "12"`, которое пин запрещает.

Два теста были поправлены, и оба раза — не подгонкой:

* помощник `_snapshot()` в `test_epics_view.py` строил «v2» без `author` и без
  трёх обязательных полей классификации. Он чинил не проверку, а фикстуру: раньше
  она проходила только потому, что парсер не проверял ничего;
* утверждение про EP-UNKNOWN намеренно не привязано к полю — инвариант в том, что
  находка НАЗВАНА, а где именно, решает исправление.

Свежесть снапшота сделала тесты зависимыми от часов, поэтому «сейчас» стало
входом `build_view(..., now=)`, а не обращением к `datetime.now()` из глубины.

## Находки ревью к самому исправлению

Copilot нашёл четыре штуки, и три из них — та же болезнь этажом выше: проверка
есть, но проверяет не то, а промах не виден.

1. **`schema_version: true` проходило.** `isinstance(True, int)` истинно и
   `True == 1`, поэтому membership-тест версии пропускал булево в модель v1, где
   lax-режим округлял его до `1`. Версия — единственное поле, которое решает, чем
   будут проверены все остальные; ей нельзя быть приблизительно верной.
2. **Ошибки загрузки учитывались только при полном провале.** Стоило одному
   снапшоту разобраться — плоскость объявляла себя `read`, хотя нечитаемый хост
   ненаблюдён ровно так же, как хост на v1. Теперь он в том же механизме
   деградации.
3. **`planeCell` работал по чёрному списку.** Всё, что не `unavailable`, рисовалось
   полным числом — то есть следующее состояние, которое отрастит сервер, поехало бы
   в прод как измеренное. Заменено белым списком: неизвестное падает в безопасную
   сторону.
4. Сообщение об ошибке публикации говорило «contract v1», хотя поддержаны обе
   версии.

Первые две закреплены тестами, третья — случаем в JS-харнессе, проверенным
мутацией.

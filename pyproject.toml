[project]
name = "dispatcher"
version = "0.1.0"
description = "Ecosystem dashboard with guarded git and config actions"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.139.0",
    "fastmcp>=2.14.5,<3",
    "jsonschema>=4.26.0",
    "plan-fields",
    "pydantic>=2.13.4",
    "pyyaml>=6.0.3",
    "ruamel-yaml>=0.19.1",
    "textual>=8.2.8,<9",
    "uvicorn>=0.49.0",
]

# The plan-fields package (PF-3) is vendored in-repo; dispatcher consumes it
# one-way (plan-fields never imports dispatcher). PF-4 uses it to resolve
# todo://<repo>/<id> for the plan_item_declared_closed evidence rule.
[tool.uv.sources]
plan-fields = { path = "packages/plan-fields", editable = true }

[project.scripts]
dispatcher = "dispatcher.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["dispatcher"]

[tool.ruff]
line-length = 88
# packages/* are standalone nested projects with their own pyproject + CI job;
# the root tool pass must not lint/format them with the root config.
extend-exclude = ["packages"]

[tool.ruff.lint]
extend-select = ["I"]

[tool.pytest.ini_options]
testpaths = ["tests"]
filterwarnings = ["ignore::pytest.PytestCollectionWarning"]

[tool.pyrefly]
project-includes = [
    "**/*.py*",
    "**/*.ipynb",
]
# packages/* are standalone nested projects (own pyproject + search-path + CI job).
project-excludes = ["packages/**"]
search-path = [".", "tests"]

[tool.prograph]
# File-based reads by dispatcher/core/collectors/*.py — invisible to
# prograph's import/MCP/contract detectors, so declared explicitly.
# Maestro's ~/.maestro/maestro.db is outside the monorepo and cannot be
# declared as a workspace-relative path.
reads = [
    "proctor/config/proctor.yaml",
    "proctor/data/state.db",
    "proctor/logs",
    "arbiter/arbiter.db",
    "arbiter/config/agents.toml",
    "arbiter/config/invariants.toml",
    "arbiter/config",
    "arbiter/logs",
    "atp-platform/.atp-dashboard.db",
    "atp-platform/results/experiment/experiment_results.json",
    "atp-platform/_bench_output",
    "atp-platform/method/agents-catalog.toml",
    "atp-platform/atp.config.yaml",
    "atp-platform/logs",
    "maestro/executor.config.yaml",
    "maestro/logs",
    "spec-runner/spec/.executor-state.db",
    "spec-runner/executor.config.yaml",
    "spec-runner/logs",
]

[dependency-groups]
dev = [
    "anyio>=4.14.1",
    "httpx>=0.28.1",
    "jsonschema>=4.26.0",
    "pyrefly>=1.1.1",
    "pytest>=9.1.1",
    "ruff>=0.15.20",
]
